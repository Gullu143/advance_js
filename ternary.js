let marks = 50
let result = (marks >= 40) ? 'pass' : 'fail'
console.log(`you ${result} the exam`);


// ternary operator in functions
const ternary = (IsCorrect)=> {
 return(IsCorrect ? "perfect" : 'not Perfect')
}
console.log(ternary(true))
console.log(ternary(false));





